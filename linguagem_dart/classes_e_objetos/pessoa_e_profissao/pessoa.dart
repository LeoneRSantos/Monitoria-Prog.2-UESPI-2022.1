class Pessoa {
  // Atributos da classe Pessoa(aquilo que caracteriza uma pessoa)

  // Em Dart, as variáveis precisam ser inicializadas por conta do null-safety(variáveis não podem ser nulas)

  String nomeDaPessoa = '';
  String cpfDaPessoa = '';

  // Para exemplificar a definição de atributos da classe individualmente, esta classe possui, apenas, o construtor padrão, que não é necessário criá-lo.

  Pessoa() {}
}
